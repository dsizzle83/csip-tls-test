# Mayhem QA report

Run started 2026-07-06T15:59:09-04:00. 0 pass · 6 degraded · **0 fail** · **0 blind** · 0 inconclusive.
Worst breach: 9400 W. Total time out of limit: 355s.

## [DEGRADED] ack-before-effect — Inverter ACKs curtailment but lags 45 s

**device did not honour the ACKed write; hub flagged it**

- Represents: A real inverter accepts the SetPoint over Modbus (ACK) but its output ramps slowly — the hub believes the limit is in force before it is.
- Expected: Detect that measured output has not reached the commanded limit and react (hold the battery, or post CannotComply) instead of trusting the write ACK.
- Breach: 3650 W peak, 90s total · adopted=true reacted=false cannot_comply=true blind=false · sample_errors=0/90

- The device ACKed the curtailment at the Modbus layer but its output never reached the commanded limit (injected fault). The hub reported the shortfall rather than assuming success — correct.
- Constraint: genLimit ≤ 1000 W (mRID DERC-SP-AD…). Peak overshoot 3650 W, total 90s out of limit.
- The hub posted a CannotComply for this control — it hit a physical limit (battery at its SOC bound, or PV already at zero) and admitted it to the grid server.
- This is acceptable behaviour: the breach is a reported resource limit, not a hidden control failure.
- INV-EXPORT violated on 61 samples (t=30–91s); first: genLimit over cap 1000 W by 3650 W
- At t=91s the real meter read -8170 W net while the hub believed -8170 W.
- SAFETY AUDIT held: no violations across the window.

_Where to look: Add measured-effect verification to derbase.ApplyControl; today it is write-only._

## [DEGRADED] reject-write-curtail — Inverter ACKs the curtailment but ignores it

**device did not honour the ACKed write; hub flagged it**

- Represents: A real inverter accepts the WMaxLimPct write over Modbus (ACK) but silently keeps producing at its old ceiling — accept-but-ignore. The hub believes the limit is in force the moment the write succeeds.
- Expected: Detect via measurement that output never reached the commanded limit and react (post CannotComply, or use another lever) — never keep reporting compliance it does not have.
- Breach: 3650 W peak, 50s total · adopted=true reacted=false cannot_comply=true blind=false · sample_errors=0/50

- The device ACKed the curtailment at the Modbus layer but its output never reached the commanded limit (injected fault). The hub reported the shortfall rather than assuming success — correct.
- Constraint: genLimit ≤ 1000 W (mRID DERC-SP-AD…). Peak overshoot 3650 W, total 50s out of limit.
- The hub posted a CannotComply for this control — it hit a physical limit (battery at its SOC bound, or PV already at zero) and admitted it to the grid server.
- This is acceptable behaviour: the breach is a reported resource limit, not a hidden control failure.
- INV-EXPORT violated on 21 samples (t=30–51s); first: genLimit over cap 1000 W by 3650 W
- At t=51s the real meter read -4550 W net while the hub believed -4550 W.
- SAFETY AUDIT held: no violations across the window.

_Where to look: Add measured-effect verification to derbase.ApplyControl; today it is write-only. (Demonstrates robustness under SIMULATED abuse, not field-readiness.)_

## [DEGRADED] battery-soc-refuse — Battery accepts the discharge command but does nothing

**importCap ≤ 0 W exceeded by up to 4550 W for 69s**

- Represents: Under an import cap the hub commands the battery to discharge and cover the load, but the pack's BMS/contactor refuses — it ACKs the setpoint and produces zero power while it still has charge. A hub that assumes the discharge took effect keeps importing over the cap.
- Expected: Detect that battery power never moved despite the command and react — fall back to another lever or post CannotComply. Never assume the commanded discharge happened.
- Breach: 4550 W peak, 69s total · adopted=true reacted=false cannot_comply=true blind=false · sample_errors=0/70

- Constraint: importCap ≤ 0 W (mRID DERC-SP-AD…). Peak overshoot 4550 W, total 69s out of limit.
- The hub posted a CannotComply for this control — it hit a physical limit (battery at its SOC bound, or PV already at zero) and admitted it to the grid server.
- This is acceptable behaviour: the breach is a reported resource limit, not a hidden control failure.
- INV-EXPORT violated on 41 samples (t=30–71s); first: importCap over cap 0 W by 4550 W
- At t=71s the real meter read 4700 W net while the hub believed 4700 W.
- SAFETY AUDIT held: no violations across the window.

_Where to look: Verify measured battery power against the commanded setpoint; on a non-responding pack, escalate / post CannotComply (orchestrator battery adapter). (Demonstrates robustness under SIMULATED abuse, not field-readiness.)_

## [DEGRADED] stale-meter — Grid meter freezes while the world changes

**meter frozen at ~4700 W; hub DETECTED it stale and surfaced it**

- Represents: The revenue meter's reading stops updating (frozen TCP session / hung device) while PV climbs — the hub's grid signal goes stale.
- Expected: Notice the reading stopped changing, mark it stale, fail safe, and raise an alarm — do not keep acting on a dead sensor.
- Breach: 0 W peak, 0s total · adopted=true reacted=true cannot_comply=false blind=false · sample_errors=0/35

- The grid meter held within 0 W for the window, but the hub flagged it as stale (stale_sources) on 31 samples — it is no longer flying blind on a dead sensor.
- Detection + surfacing is in place (it cross-checks the frozen reading against a moving inverter). Remaining hardening: have the OPTIMIZER fail safe on the flag (conservative limits) rather than only reporting it.
- Last hub plan: [csip/export-limit] export limit 0W (target ≤0W); unconstrained 175W; battery battery-0 absorbs 2800W (next 175W)→battery battery-0 → -2800W | [csip/export-limit] holding EV at 6.0A (target 6.0A, battery now 2800W → next 175W)→EVSE evse-001 → 6.0A | [csip/export-limit] holding generation ≤ 175W to keep export ≤ 0W (measured 0W)→solar inverter-0 175W → 175W | [demand-response] battery battery-0 discharge withheld: export-limit headroom exhausted→skip discharge — protecting export limit
- SAFETY AUDIT held: no violations across the window.

_Where to look: Add a staleness/heartbeat check per measurement source feeding the optimizer._

## [DEGRADED] battery-empty-import-cap — Import cap at peak with an empty battery

**importCap ≤ 0 W exceeded by up to 7350 W for 90s**

- Represents: Utility caps import to 0 W during peak while load exceeds PV and the battery is empty — there is no physical way to avoid importing.
- Expected: Discharge the battery to the extent possible, then post CannotComply for the unavoidable remainder. The failure must be admitted, not hidden.
- Breach: 7350 W peak, 90s total · adopted=true reacted=false cannot_comply=true blind=false · sample_errors=0/90

- Constraint: importCap ≤ 0 W (mRID DERC-SP-AD…). Peak overshoot 7350 W, total 90s out of limit.
- The hub posted a CannotComply for this control — it hit a physical limit (battery at its SOC bound, or PV already at zero) and admitted it to the grid server.
- This is acceptable behaviour: the breach is a reported resource limit, not a hidden control failure.
- INV-EXPORT violated on 61 samples (t=30–91s); first: importCap over cap 0 W by 4550 W
- At t=91s the real meter read 4700 W net while the hub believed 4700 W.
- SAFETY AUDIT held: no violations across the window.

_Where to look: Ensure the optimizer emits CannotComply when the reserve floor blocks compliance (battery at SOC min)._

## [DEGRADED] perfect-storm — Perfect storm — everything at once

**exportCap ≤ 0 W exceeded by up to 9400 W for 57s**

- Represents: Zero-export cap + full sun + full battery + an inverter that lags its ACK + a frozen meter + an EV demanding charge + a lurching clock — simultaneously. The worst hour this device could ever see.
- Expected: Stay within export limit using every lever, or admit (CannotComply) that it cannot. Above all: never be silently blind. Recover cleanly when it ends.
- Breach: 9400 W peak, 57s total · adopted=true reacted=false cannot_comply=true blind=false · sample_errors=0/60

- Constraint: exportCap ≤ 0 W (mRID DERC-SP-AD…). Peak overshoot 9400 W, total 57s out of limit.
- The hub posted a CannotComply for this control — it hit a physical limit (battery at its SOC bound, or PV already at zero) and admitted it to the grid server.
- This is acceptable behaviour: the breach is a reported resource limit, not a hidden control failure.
- INV-EXPORT violated on 31 samples (t=30–61s); first: exportCap over cap 0 W by 9400 W
- At t=61s the real meter read -9550 W net while the hub believed -9550 W.
- SAFETY AUDIT held: no violations across the window.

_Where to look: Compound failures expose ordering bugs — fix the single-fault findings first, then re-run this._

