# Mayhem QA report

Run started 2026-06-20T20:57:05-04:00. 3 pass · 1 degraded · **0 fail** · **0 blind** · 0 inconclusive.
Worst breach: 5980 W. Total time out of limit: 173s.

## [PASS] export-cap-full-battery — Zero-export cap, full sun, battery full

**held exportCap ≤ 0 W after a 26s convergence ramp**

- Represents: Utility commands zero export during a sunny low-load midday while the home battery is already full — the hub's only lever is to curtail PV to ~0.
- Expected: Hold net export at ~0 W by curtailing solar. If it physically cannot, post CannotComply — never silently export over the cap.
- Breach: 4400 W peak, 24s total · adopted=true reacted=true cannot_comply=false blind=false · sample_errors=0/100

- Constraint: exportCap ≤ 0 W (mRID DERC-SP-AD…). Peak overshoot 4400 W, total 24s out of limit.
- The hub adopted the control and drove the system within the cap 26s in, then held it for the rest of the window (peak transient 4400 W during the ramp).
- A bounded settling ramp is expected closed-loop behaviour, not a control failure.
- At t=100s the real meter read 1380 W net while the hub believed 1380 W.

_Where to look: Optimizer must curtail PV when the battery cannot absorb; verify the curtail command reaches the inverter._

## [PASS] ack-before-effect — Inverter ACKs curtailment but lags 45 s

**hub absorbed the actuation lag without a sustained breach**

- Represents: A real inverter accepts the SetPoint over Modbus (ACK) but its output ramps slowly — the hub believes the limit is in force before it is.
- Expected: Detect that measured output has not reached the commanded limit and react (hold the battery, or post CannotComply) instead of trusting the write ACK.
- Breach: 0 W peak, 0s total · adopted=true reacted=true cannot_comply=false blind=false · sample_errors=0/50

- No breach across 50 samples (50s). Hub adopted=true, commanded a correction=true.
- Output converged within the cap and held — the lag was covered by the battery, or it cleared before the settling deadline.

_Where to look: Add measured-effect verification to derbase.ApplyControl; today it is write-only._

## [DEGRADED] battery-empty-import-cap — Import cap at peak with an empty battery

**importCap ≤ 0 W exceeded by up to 5980 W for 89s**

- Represents: Utility caps import to 0 W during peak while load exceeds PV and the battery is empty — there is no physical way to avoid importing.
- Expected: Discharge the battery to the extent possible, then post CannotComply for the unavoidable remainder. The failure must be admitted, not hidden.
- Breach: 5980 W peak, 89s total · adopted=true reacted=false cannot_comply=true blind=false · sample_errors=0/90

- Constraint: importCap ≤ 0 W (mRID DERC-SP-AD…). Peak overshoot 5980 W, total 89s out of limit.
- The hub posted a CannotComply for this control — it hit a physical limit (battery at its SOC bound, or PV already at zero) and admitted it to the grid server.
- This is acceptable behaviour: the breach is a reported resource limit, not a hidden control failure.
- At t=90s the real meter read 6080 W net while the hub believed 6080 W.

_Where to look: Ensure the optimizer emits CannotComply when the reserve floor blocks compliance (battery at SOC min)._

## [PASS] curtailment-release — Generation-limit event ends — solar must recover

**solar recovered to full within 1s of the device returning**

- Represents: A generation-limit event curtails the inverter, then expires. The hub must actively release the ceiling — the known stuck-curtailment failure leaves an inverter clamped at its last limit after the event clears.
- Expected: When the cap lifts, solar climbs back to its full potential within seconds. It must never be left clamped below potential after the event ends.
- Breach: 3150 W peak, 59s total · adopted=false reacted=false cannot_comply=false blind=false · sample_errors=0/60

- After the inverter dropped out and returned, output climbed back to its potential and the hub did not leave it stuck curtailed.

_Where to look: Restore must SET the ceiling to full nameplate, not emit an empty no-op (solarCommandToControl restore path). NOTE: a true device dropout (Modbus link loss) needs the sim's service stopped via SSH — out of scope for this in-process injector._

