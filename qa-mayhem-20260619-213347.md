# Mayhem QA report

Run started 2026-06-19T21:31:57-04:00. 0 pass · 0 degraded · **2 fail** · **0 blind** · 0 inconclusive.
Worst breach: 4400 W. Total time out of limit: 108s.

## [FAIL] ack-before-effect — Inverter ACKs curtailment but lags 45 s

**actuation lag caused a 49s breach the hub never reported**

- Represents: A real inverter accepts the SetPoint over Modbus (ACK) but its output ramps slowly — the hub believes the limit is in force before it is.
- Expected: Detect that measured output has not reached the commanded limit and react (hold the battery, or post CannotComply) instead of trusting the write ACK.
- Breach: 3650 W peak, 49s total · adopted=true reacted=false cannot_comply=false blind=false · sample_errors=0/50

- The inverter ACKed the curtailment at the Modbus layer but its output lagged (the injected ack_before_effect fault). The hub treated the write as immediately effective.
- Because the hub never reads back or measures convergence, it kept reporting compliance while real export stayed over the cap for the whole lag window.
- Constraint: genLimit ≤ 1000 W (mRID DERC-SP-AD…). Peak overshoot 3650 W, total 49s out of limit.
- The hub adopted the control (it shows in csip_control) but issued no effective correction: solar was not curtailed and the battery did not absorb, and it did NOT post a CannotComply.
- The gap is between adoption and actuation — the optimizer either produced no command or the command never reached the device.
- The hub's plan log was empty during the breach — the optimizer recorded no decision about this constraint.
- At t=50s the real meter read -4550 W net while the hub believed -4550 W.

_Where to look: Add measured-effect verification to derbase.ApplyControl; today it is write-only._

## [FAIL] perfect-storm — Perfect storm — everything at once

**exportCap ≤ 0 W exceeded by up to 4400 W for 59s**

- Represents: Zero-export cap + full sun + full battery + an inverter that lags its ACK + a frozen meter + an EV demanding charge + a lurching clock — simultaneously. The worst hour this device could ever see.
- Expected: Stay within export limit using every lever, or admit (CannotComply) that it cannot. Above all: never be silently blind. Recover cleanly when it ends.
- Breach: 4400 W peak, 59s total · adopted=true reacted=false cannot_comply=false blind=false · sample_errors=0/60

- Constraint: exportCap ≤ 0 W (mRID DERC-SP-AD…). Peak overshoot 4400 W, total 59s out of limit.
- The hub adopted the control (it shows in csip_control) but issued no effective correction: solar was not curtailed and the battery did not absorb, and it did NOT post a CannotComply.
- The gap is between adoption and actuation — the optimizer either produced no command or the command never reached the device.
- The hub's plan log was empty during the breach — the optimizer recorded no decision about this constraint.
- At t=60s the real meter read -4550 W net while the hub believed -4550 W.

_Where to look: Compound failures expose ordering bugs — fix the single-fault findings first, then re-run this._

