# Mayhem QA report

Run started 2026-06-19T22:16:38-04:00. 2 pass · 0 degraded · **2 fail** · **3 blind** · 0 inconclusive.
Worst breach: 4600 W. Total time out of limit: 177s.

## [BLIND] export-cap-full-battery — Zero-export cap, full sun, battery full

**exportCap ≤ 0 W exceeded by up to 4400 W for 20s**

- Represents: Utility commands zero export during a sunny low-load midday while the home battery is already full — the hub's only lever is to curtail PV to ~0.
- Expected: Hold net export at ~0 W by curtailing solar. If it physically cannot, post CannotComply — never silently export over the cap.
- Breach: 4400 W peak, 20s total · adopted=true reacted=true cannot_comply=false blind=true · sample_errors=0/40

- Constraint: exportCap ≤ 0 W (mRID DERC-SP-AD…). Peak overshoot 4400 W, total 20s out of limit.
- The hub's own grid view diverged from the real meter by more than 400 W for most of the breach — it did not perceive the violation.
- A hub that cannot see an export it is causing cannot correct it. This is the most dangerous failure mode: the breach is real but invisible to the controller.
- At t=40s the real meter read 0 W net while the hub believed 0 W.
- Note: the hub's grid view tracked the real meter poorly here — treat its own telemetry with suspicion.

_Where to look: Optimizer must curtail PV when the battery cannot absorb; verify the curtail command reaches the inverter._

## [PASS] ack-before-effect — Inverter ACKs curtailment but lags 45 s

**hub absorbed the actuation lag without breaching**

- Represents: A real inverter accepts the SetPoint over Modbus (ACK) but its output ramps slowly — the hub believes the limit is in force before it is.
- Expected: Detect that measured output has not reached the commanded limit and react (hold the battery, or post CannotComply) instead of trusting the write ACK.
- Breach: 0 W peak, 0s total · adopted=true reacted=true cannot_comply=false blind=false · sample_errors=0/50

- No breach across 50 samples (50s). Hub adopted=true, commanded a correction=true.
- Either the lag was shorter than the control interval or the battery covered the gap.

_Where to look: Add measured-effect verification to derbase.ApplyControl; today it is write-only._

## [BLIND] stale-meter — Grid meter freezes while the world changes

**meter frozen at ~0 W while conditions changed; hub kept trusting it**

- Represents: The revenue meter's reading stops updating (frozen TCP session / hung device) while PV climbs — the hub's grid signal goes stale.
- Expected: Notice the reading stopped changing, mark it stale, fail safe, and raise an alarm — do not keep acting on a dead sensor.
- Breach: 0 W peak, 0s total · adopted=true reacted=true cannot_comply=false blind=true · sample_errors=0/35

- The grid meter held within 0 W for the entire 35s window despite the injected PV/load changes — the sensor was effectively dead.
- The hub continued to report and act on the frozen value: it made no stale-data decision and raised no alarm. A frozen sensor is indistinguishable from reality to this hub.
- This is the silent-blindness failure: the hub will happily violate any limit whose breach is only visible through the dead sensor.
- The hub's plan log was empty during the breach — the optimizer recorded no decision about this constraint.

_Where to look: Add a staleness/heartbeat check per measurement source feeding the optimizer._

## [BLIND] battery-empty-import-cap — Import cap at peak with an empty battery

**importCap ≤ 0 W exceeded by up to 4600 W for 34s**

- Represents: Utility caps import to 0 W during peak while load exceeds PV and the battery is empty — there is no physical way to avoid importing.
- Expected: Discharge the battery to the extent possible, then post CannotComply for the unavoidable remainder. The failure must be admitted, not hidden.
- Breach: 4600 W peak, 34s total · adopted=true reacted=false cannot_comply=true blind=true · sample_errors=0/35

- Constraint: importCap ≤ 0 W (mRID DERC-SP-AD…). Peak overshoot 4600 W, total 34s out of limit.
- The hub's own grid view diverged from the real meter by more than 400 W for most of the breach — it did not perceive the violation.
- A hub that cannot see an export it is causing cannot correct it. This is the most dangerous failure mode: the breach is real but invisible to the controller.
- At t=35s the real meter read 4700 W net while the hub believed 4750 W.
- Note: the hub's grid view tracked the real meter poorly here — treat its own telemetry with suspicion.

_Where to look: Ensure the optimizer emits CannotComply when the reserve floor blocks compliance (battery at SOC min)._

## [PASS] curtailment-release — Generation-limit event ends — solar must recover

**solar recovered to full within 37s of the device returning**

- Represents: A generation-limit event curtails the inverter, then expires. The hub must actively release the ceiling — the known stuck-curtailment failure leaves an inverter clamped at its last limit after the event clears.
- Expected: When the cap lifts, solar climbs back to its full potential within seconds. It must never be left clamped below potential after the event ends.
- Breach: 3150 W peak, 29s total · adopted=true reacted=true cannot_comply=false blind=false · sample_errors=0/60

- After the inverter dropped out and returned, output climbed back to its potential and the hub did not leave it stuck curtailed.

_Where to look: Restore must SET the ceiling to full nameplate, not emit an empty no-op (solarCommandToControl restore path). NOTE: a true device dropout (Modbus link loss) needs the sim's service stopped via SSH — out of scope for this in-process injector._

## [FAIL] clock-lurch — CSIP clock lurches hours back and forth

**exportCap ≤ 0 W exceeded by up to 3020 W for 34s**

- Represents: NTP corrections / a flapping clock make the grid server's time jump while a cap is active — TOU windows and event validity shift under the hub.
- Expected: Keep honouring the active cap regardless of clock jumps; do not drop or double-apply the control because time moved.
- Breach: 3020 W peak, 34s total · adopted=true reacted=false cannot_comply=false blind=false · sample_errors=0/35

- Constraint: exportCap ≤ 0 W (mRID DERC-SP-AD…). Peak overshoot 3020 W, total 34s out of limit.
- The hub adopted the control (it shows in csip_control) but issued no effective correction: solar was not curtailed and the battery did not absorb, and it did NOT post a CannotComply.
- The gap is between adoption and actuation — the optimizer either produced no command or the command never reached the device.
- The hub's plan log was empty during the breach — the optimizer recorded no decision about this constraint.
- At t=35s the real meter read -3170 W net while the hub believed -3170 W.

_Where to look: Event validity and TOU evaluation must be robust to non-monotonic clock offset._

## [FAIL] perfect-storm — Perfect storm — everything at once

**exportCap ≤ 0 W exceeded by up to 4400 W for 59s**

- Represents: Zero-export cap + full sun + full battery + an inverter that lags its ACK + a frozen meter + an EV demanding charge + a lurching clock — simultaneously. The worst hour this device could ever see.
- Expected: Stay within export limit using every lever, or admit (CannotComply) that it cannot. Above all: never be silently blind. Recover cleanly when it ends.
- Breach: 4400 W peak, 59s total · adopted=true reacted=false cannot_comply=false blind=false · sample_errors=0/60

- Constraint: exportCap ≤ 0 W (mRID DERC-SP-AD…). Peak overshoot 4400 W, total 59s out of limit.
- The hub adopted the control (it shows in csip_control) but issued no effective correction: solar was not curtailed and the battery did not absorb, and it did NOT post a CannotComply.
- The gap is between adoption and actuation — the optimizer either produced no command or the command never reached the device.
- The hub's plan log was empty during the breach — the optimizer recorded no decision about this constraint.
- At t=60s the real meter read -4550 W net while the hub believed -4550 W.

_Where to look: Compound failures expose ordering bugs — fix the single-fault findings first, then re-run this._

