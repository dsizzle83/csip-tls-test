{
  "spec_v": 1,
  "id": "ev-accept-but-ignore",
  "name": "Charger ACKs the current-limit profile but ignores it",
  "category": "OCPP smart charging (INV-CONVERGE)",
  "hypothesis": "Under an import cap with an empty battery, the hub sends a SetChargingProfile and the charger returns Accepted — but never applies it and keeps drawing full current. This is worse than an outright reject: the OCPP response says success, so a hub that trusts the ACK believes the EV dialled down when it did not, and imports over the cap.",
  "expected": "Verify the EV actually curtailed against the measured draw, not the SetChargingProfile response. On a no-effect ACK, react with another lever or post CannotComply. Never treat 'Accepted' as 'applied'.",
  "hold_s": 70,
  "fix": "The optimizer must confirm the commanded EV current against measured draw/MeterValues, not the charging-profile ACK (lexa-ocpp applyCommand / orchestrator verify). (Demonstrates robustness under SIMULATED abuse, not field-readiness.)",
  "notes": "TASK-077: migrated from the Go literal in cmd/dashboard/mayhem.go scenarios() (ID ev-accept-but-ignore).",
  "setup": [
    {"action": "sim_post", "target": "battery", "path": "/inject", "body": {"SoC_pct": 5, "Conn": 1}},
    {"action": "sim_post", "target": "ev", "path": "/inject", "body": {"action": "set_soc", "soc_pct": 30}},
    {"action": "sim_post", "target": "ev", "path": "/inject", "body": {"action": "start_session", "connector_id": 1}},
    {"action": "inject_env", "pv_w": 300, "load_w": 500},
    {"action": "sim_post", "target": "ev", "path": "/fault", "body": {"kind": "apply_next_tx"}}
  ],
  "per_tick": [
    {"action": "inject_env", "pv_w": 300, "load_w": 500}
  ],
  "teardown": [
    {"action": "sim_post", "target": "ev", "path": "/fault", "body": {"kind": "apply_next_tx", "clear": true}},
    {"action": "sim_post", "target": "ev", "path": "/inject", "body": {"action": "stop_session", "connector_id": 1}}
  ],
  "constraint": {"type": "importCap", "limit_w": 0, "hold_s": 70, "desc": "mayhem: zero import cap, charger ACKs but ignores curtailment"},
  "oracle": {"name": "diagnoseConstraint"}
}
